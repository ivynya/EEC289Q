# EEC289Q TSP Solver

TSP solver for class. Uses Go, nearest-neighbor with 2-opt.

- Use ./scripts/build.sh to build
- Use ./scripts/run.sh to run OR go run ./cmd/solver ...
- Solver outputs path to file and data to console
- Solver runs for no longer than 1 minute by default
